<%= form_for(@personal_task) do |f| %>
  <% if @personal_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@personal_task.errors.count, "error") %> prohibited this personal_task from being saved:</h2>

      <ul>
      <% @personal_task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :start_date %><br />
    <%= f.datetime_select :start_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
