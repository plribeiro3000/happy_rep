<%= form_for(@resident, :url => resident_path(@resident)) do |f| %>
    <%= render :partial => 'form', :locals => { :f => f } %>
<% end %>

<%= link_to 'Visualizar', resident_path(@resident) %> |
<%= link_to 'Voltar', residents_path %>