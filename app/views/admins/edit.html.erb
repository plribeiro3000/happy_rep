<%= form_for(@admin, :url => admin_path(@admin)) do |f| %>
    <%= render :partial => 'form', :locals => { :f => f } %>
<% end %>

<%= link_to 'Visualizar', admin_path(@admin) %> |
<%= link_to 'Voltar', admins_path %>