<table>
  <thead>
  <tr>
    <th>Nome</th>
    <th></th>
    <th></th>
  </tr>
  </thead>

  <tbody>
  <% @admins.each do |admin| %>
      <tr>
        <td><%= admin.profile.name %></td>
        <td><%= link_to "Detalhar", admin_path(admin) %></td>
        <td><%= link_to "Apagar", admin, :data => { :confirm => "Você tem certeza?" }, :method => :delete %></td>
      </tr>
  <% end %>
  </tbody>
</table>