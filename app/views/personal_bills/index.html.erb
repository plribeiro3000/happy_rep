<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Value</th>
    <th>Payment date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @personal_bills.each do |personal_bill| %>
  <tr>
    <td><%= personal_bill.name %></td>
    <td><%= personal_bill.description %></td>
    <td><%= personal_bill.value %></td>
    <td><%= personal_bill.payment_date %></td>
    <td><%= link_to 'Show', personal_bill %></td>
    <td><%= link_to 'Edit', edit_personal_bill_path(personal_bill) %></td>
    <td><%= link_to 'Destroy', personal_bill, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Personal bill', new_personal_bill_path %>