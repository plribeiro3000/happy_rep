<%= form_tag(loans_path, :method => "get") do %>
    <div class="container-fluid">
      <div class="row-fluid" >
        <div class="span3">
          Devedor:
        </div>
        <div class="span3 row-form">
          <input type="text" id="dweller" name="dweller" />
        </div>
      </div>
      <div class="row-fluid" >
        <div class="span3">
          Data de realizaçao:
        </div>
        <div class="span3 row-form">
          <input type="text" id="date" name="date" placeholder="Exemplo: 01/01/1992" />
        </div>
      </div>
    </div>
    <%= button_tag("Consultar") %>

<% end %>


<table class="table table-striped">
<tr>
    <th>Realization date</th>
    <th>Value</th>
    <th>Dweller</th>
    <th>Payment date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @loans.each do |loan| %>
  <tr>
    <td><%= loan.realization_date %></td>
    <td><%= loan.value %></td>
    <td><%= loan.dweller %></td>
    <td><%= loan.payment_date %></td>
    <td><%= link_to 'Show', loan %></td>
    <td><%= link_to 'Edit', edit_loan_path(loan) %></td>
    <td><%= link_to 'Destroy', loan, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Loan', new_loan_path %>