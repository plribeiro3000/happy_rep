<%= form_for(@former, :url => former_path(@former)) do |f| %>
    <%= render :partial => 'form', :locals => { :f => f } %>
<% end %>

<%= link_to 'Show', @former %> |
<%= link_to 'Back', formers_path %>