<%= form_for(@former, :url => former_path(@former)) do |f| %>
    <%= render :partial => 'form', :locals => { :f => f } %>
<% end %>

<%= link_to 'Visualizar', former_path(@former) %> |
<%= link_to 'Voltar', formers_path %>