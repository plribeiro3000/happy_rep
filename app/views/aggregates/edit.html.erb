<%= form_for(@aggregate, :url => aggregate_path(@aggregate)) do |f| %>
    <%= render :partial => 'form', :locals => { :f => f } %>
<% end %>

<%= link_to 'Visualizar', aggregate_path(@aggregate) %> |
<%= link_to 'Voltar', aggregates_path %>